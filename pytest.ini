[pytest]
pythonpath = .
testpaths = tests
python_files = test_*.py
addopts = -v -s --color=yes --cov=cortex --cov-report=term-missing

markers =
    unit: pruebas unitarias (rápidas, sin conexión)
    integration: pruebas de integración (usan conexión real)
